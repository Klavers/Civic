# Codex workspace

이 디렉토리는 저장소와 함께 공유할 Codex 작업 산출물의 루트다.

- `handoffs/`: `session-handoff` 스킬이 생성하는 세션 인계 문서
- 개인 설정 `settings.local.json`과 임시 파일 `tmp/`는 저장소 루트 `.gitignore`로 제외

프로젝트 공통 작업 규칙과 스킬 맵의 정본은 저장소 루트 `AGENTS.md`다.
