# Session Handoff — {YYYY-MM-DD} ({TOPIC})

> 이전 세션: `handoff_{YYYYMMDD_prev}_{prev_topic}.md` (있으면 링크, 없으면 삭제)
> 본 파일 목적: 다음 Claude 세션이 이전 세션의 맥락을 완전히 흡수하여 **자연스럽게 이어서 작업**할 수 있게 함.

---

## § 1. 세션 요약

**기간**: {시작일} ~ {종료일}

**최대 성과**:
1. {성과 1 — 명확한 산출물}
2. {성과 2}
3. {성과 3}

**머지된 PR**:

| # | 제목 | 머지일 | 내용 |
|-|-|-|-|
| #NN | {title} | {YYYY-MM-DD} | {1줄 요약} |

---

## § 2. 현재까지 구현된 사항

### 코드 (병합 완료)
- {병합 완료된 작업 범위}
- **최근 세션 기여**: {있으면 구체 기재, 없으면 "없음 (문서/준비 세션)"}

### 문서 (최근 PR로 반영)
- `{path}` — {1줄 요약}

### CLAUDE.md 갱신 섹션
- {섹션 이름}

---

## § 3. 향후 수행 우선순위

### 1순위: {작업 이름} (즉시 착수 가능)

**상태**: {사전 조사 완료 / 구현 착수 준비 등}

**핵심 사실**:
- {중요 수치·경로·전제}

**PR 구조** (예상 N 커밋):
1. {Commit 1 — 요지}
2. {Commit 2 — 요지}

**테스트 전략**:
- {test file path} — {검증 내용}

**Branch 명 후보**: `{slug}`

---

### 2순위 이하

| 순위 | 작업 | 의존 |
|-|-|-|
| 2 | {작업} | {선행} |
| 3 | {작업} | {선행} |

### 보류 항목

- **{작업 이름}**: {보류 사유} — {재개 조건}

---

## § 4. 사용자 성향 / 세션 운영 스타일 (이번 세션에서 확립·강화된 것만)

> CLAUDE.md의 협업 규범은 링크로 참조. 여기에는 **이번 세션에서 새로 확인된 것 또는 강화된 패턴만** 기재.

- **{성향 1}**: {구체 예시 — 이번 세션 어느 시점에 드러났는지}
- **{성향 2}**: {구체 예시}

세션 운영 스타일 특이점:
- {특이점}

---

## § 5. 이번 세션에서 학습·기억한 사항

### 프로젝트 구조 핵심 (이번 세션에서 처음 확정된 것)
- {항목}

### 프로젝트 핵심 수치 (단일 소스 포인터)
- {수치 이름}: {값} — **단일 소스**: `{path}`

### 기술 스택 유의사항
- {유의점}

### 세션 도구 팁
- {팁 — 예: "gh CLI 경로", 셸 제약 등}

---

## § 6. 다음 세션 착수 가이드

다음 세션이 "핸드오프 메모 확인" 또는 유사 지시를 받으면 **이 순서대로** 실행:

1. **CLAUDE.md 읽기** — 특히 다음 섹션:
   - {섹션 1}
   - {섹션 2}
2. **본 파일 읽기** — `.claude/handoffs/handoff_{YYYYMMDD}_{topic}.md`
3. **관련 문서 재독** — `{plan/문서 경로}`
4. **코드 진입점 재확인**:
   - `{module/file}:{NN-MM}` — {역할}
5. **Branch 생성**:
   ```bash
   git checkout {base-branch}
   git pull
   git checkout -b {slug}
   ```
6. **첫 커밋 착수**: {Commit 1 구체 지시}

### 주의사항 (실수 방지)
- ⚠️ `git add .` / `-A` 금지. 생성 파일만 명시 경로로 스테이징.
- ⚠️ {이번 세션 고유 주의사항}

---

## § 7. 참조 경로 치트시트

- 프로젝트 규범: `{repo-root}/CLAUDE.md`
- Plan root: `{repo-root}/docs/plan/` (있을 경우)
- 핸드오프 루트: `{repo-root}/.claude/handoffs/`
- 이전 세션 노트: `{경로}` (있으면)
- {핵심 모듈·테스트 진입점 경로}

---

*생성: {YYYY-MM-DD}*
